

describe('FileUploadComponent', () => {
  // tests.spec.ts

describe('Get Candidate details', () => {
  it('should return 4 when adding 2 + 2', () => {
    const result = 2 + 2;
    expect(result).toBe(4);
  });
});

describe('Get details of a candidate not in the database', () => {
  it('should return the correct length of a string', () => {
    const str = 'Hello, World!';
    const length = str.length;
    expect(length).toBe(13);
  });
});

describe('Get details of a candidate in the database', () => {
  it('should contain the value 3 in the array', () => {
    const arr = [1, 2, 3, 4, 5];
    expect(arr).toContain(3);
  });
});



});
